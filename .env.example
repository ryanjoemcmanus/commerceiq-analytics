# Optional PostgreSQL settings for a later project phase.
# Copy this file to .env and replace the example values locally.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=commerceiq
DB_USER=postgres
DB_PASSWORD=replace_me

