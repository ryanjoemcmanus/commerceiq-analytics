[tool.ruff]
target-version = "py311"
line-length = 100
extend-exclude = ["notebooks", "tmp", "data"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "B", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
